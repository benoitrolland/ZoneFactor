import { ZoneSlidersDirective } from './zone-sliders.directive';

describe('ZoneSlidersDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoneSlidersDirective();
    expect(directive).toBeTruthy();
  });
});
