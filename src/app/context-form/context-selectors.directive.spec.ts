import { ContextSelectorsDirective } from './context-selectors.directive';

describe('ContextSelectorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextSelectorsDirective();
    expect(directive).toBeTruthy();
  });
});
